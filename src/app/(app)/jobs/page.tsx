import { AppPlaceholder } from "@/components/layout/app-placeholder";
export default function Page() {
  return (
    <AppPlaceholder
      title="Jobs"
      description="The future workspace for supported opportunity discovery."
    />
  );
}
