import { AppPlaceholder } from "@/components/layout/app-placeholder";
export default function Page() {
  return (
    <AppPlaceholder
      title="Profile"
      description="The future candidate-controlled source of truth."
    />
  );
}
