import { AppPlaceholder } from "@/components/layout/app-placeholder";
export default function Page() {
  return (
    <AppPlaceholder
      title="Settings"
      description="The future home for preferences, authority, and integrations."
    />
  );
}
