import { AppPlaceholder } from "@/components/layout/app-placeholder";
export default function Page() {
  return (
    <AppPlaceholder
      title="Applications"
      description="The future record of prepared and submitted applications."
    />
  );
}
